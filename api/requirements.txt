# Pinned to the versions this project was built and measured against, so a
# rebuild months from now produces the same image rather than silently picking
# up a new major of openai or sqlalchemy.
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
python-multipart==0.0.32
openai==2.53.0
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
pypdfium2==5.12.1
pillow==12.3.0
python-dateutil==2.9.0.post0
schwifty==2026.7.3
